<UserControl x:Class="FgoPet.App.Settings.Views.SettingsNavigationView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
<UserControl.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="/FgoPet.App;component/Ui/Shell/ChatControls.xaml"/></ResourceDictionary.MergedDictionaries>
 <StreamGeometry x:Key="AppearanceIcon">M8,2 A3,3 0 1 1 8,8 A3,3 0 1 1 8,2 M2,15 V13 Q2,10 8,10 Q14,10 14,13 V15</StreamGeometry>
 <StreamGeometry x:Key="ModelIcon">M2,2 H14 V7 H2 Z M2,10 H14 V15 H2 Z M5,4 V5 M5,12 V13</StreamGeometry>
 <StreamGeometry x:Key="AgentIcon">M3,8 H13 M3,8 L6,5 M3,8 L6,11 M13,8 L10,5 M13,8 L10,11</StreamGeometry>
 <StreamGeometry x:Key="DataIcon">M2,5 L8,2 L14,5 L8,8 Z M2,9 L8,12 L14,9 M2,13 L8,16 L14,13</StreamGeometry>
</ResourceDictionary></UserControl.Resources>
<ListBox x:Name="NavigationList" SelectedValuePath="Section" BorderThickness="0" Background="Transparent"
 ScrollViewer.HorizontalScrollBarVisibility="Disabled" AutomationProperties.Name="设置分类">
 <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem">
  <Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Margin" Value="0,0,0,8"/><Setter Property="Padding" Value="12,10"/>
  <Setter Property="Foreground" Value="{DynamicResource ShellTextBrush}"/><Setter Property="Cursor" Value="Hand"/><Setter Property="FocusVisualStyle" Value="{x:Null}"/>
  <Setter Property="AutomationProperties.Name" Value="{Binding Label}"/>
  <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem">
   <Border x:Name="Chrome" CornerRadius="8" BorderThickness="1" BorderBrush="Transparent" Background="Transparent" Padding="{TemplateBinding Padding}"><ContentPresenter/></Border>
   <ControlTemplate.Triggers>
    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ShellRaisedBackgroundBrush}"/></Trigger>
    <Trigger Property="IsSelected" Value="True"><Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ShellRaisedBackgroundBrush}"/><Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource ShellAccentBrush}"/><Setter Property="Foreground" Value="{DynamicResource ShellAccentBrush}"/></Trigger>
    <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource ShellAccentBrush}"/></Trigger>
   </ControlTemplate.Triggers>
  </ControlTemplate></Setter.Value></Setter>
 </Style></ListBox.ItemContainerStyle>
 <ListBox.ItemTemplate><DataTemplate>
  <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
   <Path Width="16" Height="16" Stretch="Uniform" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round">
    <Path.Style><Style TargetType="Path"><Setter Property="Data" Value="{StaticResource AppearanceIcon}"/><Style.Triggers>
     <DataTrigger Binding="{Binding IconKey}" Value="Model"><Setter Property="Data" Value="{StaticResource ModelIcon}"/></DataTrigger>
     <DataTrigger Binding="{Binding IconKey}" Value="Speech"><Setter Property="Data" Value="{StaticResource ChatSpeak}"/></DataTrigger>
     <DataTrigger Binding="{Binding IconKey}" Value="Agent"><Setter Property="Data" Value="{StaticResource AgentIcon}"/></DataTrigger>
     <DataTrigger Binding="{Binding IconKey}" Value="Data"><Setter Property="Data" Value="{StaticResource DataIcon}"/></DataTrigger>
    </Style.Triggers></Style></Path.Style>
   </Path>
   <TextBlock Grid.Column="1" Text="{Binding Label}" Margin="10,0,0,0" VerticalAlignment="Center"/>
  </Grid>
 </DataTemplate></ListBox.ItemTemplate>
</ListBox>
</UserControl>