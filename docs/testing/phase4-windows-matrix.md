# Phase 4 closeout：用户桌面验收

本轮桌面操作全部由用户执行并通过截图反馈。以下记录区分用户界面证据与自动化测试证据。
使用独立测试数据，不修改日常数据、用户 PATH 或正式插件配置。

2026-09-01 机器门禁已通过：Release 构建 0 warning / 0 error，.NET 697/697，Python 105/105。用户桌面 M1–M4 已完成，测试数据库只读核对通过。本轮主要 Windows 环境验收关闭，不代表完整发行或设备矩阵验收。

## 候选启动前

- 先完成候选的代码评审与机器测试，再提供启动入口，避免重复验收中间版本。
- 关闭旧桌宠后启动候选。确认进程、配对、角色包和任务使用的是隔离数据目录。
- 最终候选使用独立 `state-final` 与 `closeout-final-20260901` 管道，不复用旧测试 Relay；未复制 Relay 授权或 Adapter 凭据。首次启动需由用户批准新测试来源。
- 测试目标是专门登记的只读空项目；合成任务只回复固定文字，不操作文件或运行命令。

## 最小验收步骤

| 编号 | 用户操作 | 预期 | 本轮结果 |
|---|---|---|---|
| M1 | 在 Agent 连接设置中批准新测试来源；仅允许已登记测试目标 `project-85e5d2d9ae46aee1bfa2caff`，启用来源并核对派发确认框 | App、Relay、Adapter 在线；来源与项目权限明确 | 通过：用户批准，来源在线，确认框目标与合成任务一致 |
| M2 | TODO 中选择预置 `Phase4 completion acceptance`，点击“交给 Agent”，核对唯一测试目标后确认派发 | 任务最终显示已完成，不一直停在执行中 | 通过：用户截图显示已完成；数据库记录 started→updated→completed |
| M3 | 退出桌宠，重新启动同一候选，查看 TODO 和 Agent 设置 | 已完成任务保持完成；同一来源恢复连接，无需重复批准 | 通过：重启后完成态保持，三端在线，无待批准请求 |
| M4 | 撤销这个测试来源，退出并重启候选 | 旧授权不恢复，来源需重新批准 | 通过：截图显示等待批准、适配器离线，同一来源产生新待批准请求；未授权派发拒绝由自动测试覆盖，本轮未尝试再次派发 |

若 M2 无法完成，反馈当前状态文字或截图即可，不反复点击确认派发。若按钮找不到，先记录具体页面，不使用猜测坐标自动点击。

运行中取消/权限撤销的机器测试必须验证停止执行及可解释终态；若需要补实机检查，应另外准备合成任务并请用户操作，不能后台启动账户任务。

## 数据核对

本轮已用 SQLite `mode=ro` 与 `query_only=ON` 查询 `state-final/runtime.db`，没有读取配对凭据：

- Todo：`phase4-closeout-completion-20260901`，状态 `completed`。
- 唯一 dispatch：`dispatch-3532018554e5f5dfb429b494`；唯一 execution 状态 `completed`。
- 事件：sequence 1 `task_started`、2 `task_updated`、3 `task_completed`；无重复序号，完成事件仅一次。
- 完成时间：2026-09-01 01:52:54（Asia/Shanghai）；Todo 完成时间与 execution 结束时间一致。
- 用户配对来源：`codex-9b1ba8d761964f1d9610f9a7c98da3ca`。撤销重启后的截图显示该来源重新等待批准。

截图证据位于当前任务附件；未将含桌面背景的截图加入仓库。完成态截图后缀 `f82d656f`，重启完成态 `69a3fd89`，重连 `6353f4ff`，撤销重启 `a2e75815`。

收到 M2/M3 反馈后，对隔离 SQLite 做只读查询，核对同一 Todo 与 Agent execution
最终状态、完成时间、事件去重；查询成功不替代用户看到正确界面的证据。

M4 后检查隔离状态的来源状态及重启结果，不输出凭据或解密内容。
只删除或清理本轮明确创建的测试数据；正常用户数据不在范围内。

## 历史证据的使用范围

`../superpowers/plans/2026-08-31-phase4-final-acceptance.md` 记录了修复前的真实
Codex transport、重启与撤销结果，但其中旧任务仍卡在 App 投影，不算 M2 通过。
本轮第一次配对在自动化点击前已完成，来源尚未确认，因此不能写成“自动化批准成功”。

## 关闭条件

核心评审问题关闭、候选机器门禁通过、M1–M4 有明确用户反馈，且数据库与界面一致，
才把 Phase 4 标为 accepted。否则明确区分“代码候选已准备”和“用户桌面验收未完成”。
