<Project>

  <!--
    实验工程的显式退出：故意中断继承。

    MSBuild 的规则是：每个工程从自己所在目录向上查找 Directory.Build.props，
    **命中第一个就停止**。所以本文件的存在会让 spikes/ 下的实验工程不再加载仓库根的
    编译基线（Ⅲ-5），保持它们原有的"零全局属性"状态。

    原因：实验代码不承担产品的分层与可见性约束，全局开启 Nullable 会给它们引入
    大量与本轮重构无关的新诊断，污染实验信号的读取。

    删除本文件会让实验工程重新继承根目录基线。要修改实验工程的编译行为时，
    直接写在本文件里即可。
  -->

</Project>
